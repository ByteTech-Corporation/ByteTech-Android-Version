package android.abstractdatavector;

import java.io.*;
import java.*;

public class AbstractDatavector {
    public static void main(String, args[]) {
        abstract static void Datavector(String, args[]) {
            @Override
            /*
             * @param Datavector[int];
             * @param Datavector.Data[char data = ""];
             */

            int datavector = String datavector = {library.Datavector(hex().getInput().getProc())}
        }
    }
}
