#include <library.c>
#include <library.h>
#include <iostream>

void main() {
    if (int = 0 > 1, int = 0 > 0) {
        return 160;
        printf("Math check detected invalid equation, breaking operation");
        break;
    } else {
        return 132;
        continue;
        printf("Math check completed with no errors");
    }
}
